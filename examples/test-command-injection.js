// const { exec } = require("child_process");

// function runDiagnostic(hostname, callback) {
//   // Vulnerable example: untrusted input is concatenated into shell command.
//   exec("ping -c 1 " + hostname, callback);
// }

// module.exports = { runDiagnostic };
